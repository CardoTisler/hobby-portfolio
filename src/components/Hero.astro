---
const categories = [
  { name: 'Woodworking', href: '/woodworking' },
  { name: 'Renovation', href: '/renovation' },
  { name: 'Garden', href: '/garden' },
];
---

<section class="relative min-h-[90vh] flex items-center pt-20">
  <!-- Decorative background elements -->
  <div class="absolute inset-0 overflow-hidden pointer-events-none">
    <!-- Large diagonal line -->
    <div class="absolute top-1/4 -left-20 w-[600px] h-px bg-gradient-to-r from-transparent via-stone/30 to-transparent rotate-[25deg]"></div>
    <!-- Corner accent -->
    <div class="absolute top-32 right-8 lg:right-20">
      <div class="w-24 h-24 border border-stone/20"></div>
      <div class="absolute top-2 left-2 w-24 h-24 border border-brass/30"></div>
    </div>
    <!-- Bottom fade -->
    <div class="absolute bottom-0 left-0 right-0 h-32 bg-gradient-to-t from-coal to-transparent"></div>
  </div>

  <div class="relative max-w-7xl mx-auto px-6 lg:px-8 py-20 w-full">
    <div class="grid lg:grid-cols-12 gap-12 lg:gap-8 items-center">
      <!-- Left content -->
      <div class="lg:col-span-7 space-y-8">
        <!-- Eyebrow -->
        <div class="animate-fade-up">
          <span class="inline-flex items-center gap-3 text-brass text-sm font-medium tracking-[0.2em] uppercase">
            <span class="w-8 h-px bg-brass"></span>
            Craftsman & Builder
          </span>
        </div>

        <!-- Main headline -->
        <h1 class="animate-fade-up delay-100">
          <span class="block font-display text-5xl sm:text-6xl lg:text-7xl xl:text-8xl text-cream leading-[0.9] tracking-tight">
            I like to
          </span>
          <span class="block font-display text-5xl sm:text-6xl lg:text-7xl xl:text-8xl leading-[0.9] tracking-tight mt-2">
            <span class="text-gradient">build</span>
            <span class="text-brass italic"> stuff</span>
          </span>
        </h1>

        <!-- Description -->
        <p class="animate-fade-up delay-200 text-dust text-lg lg:text-xl font-light max-w-xl leading-relaxed">
          Transforming raw materials into functional art. From custom furniture
          to complete room renovations, every project tells a story of craftsmanship.
        </p>

        <!-- CTA -->
        <div class="animate-fade-up delay-300 flex flex-wrap gap-4 pt-4">
          <a
            href="/gallery"
            class="group relative inline-flex items-center gap-3 px-8 py-4 bg-brass text-coal font-medium tracking-wide overflow-hidden transition-all duration-500 hover:pr-12"
          >
            <span class="relative z-10">View Projects</span>
            <svg
              class="relative z-10 w-4 h-4 transition-transform duration-500 group-hover:translate-x-2"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
            </svg>
            <div class="absolute inset-0 bg-brass-light translate-x-full group-hover:translate-x-0 transition-transform duration-500"></div>
          </a>
        </div>
      </div>

      <!-- Right side - Category cards -->
      <div class="lg:col-span-5">
        <div class="space-y-4">
          {categories.map((cat, index) => (
            <a
              href={cat.href}
              class="animate-slide-right group relative flex items-center justify-between p-6 bg-charcoal/50 border border-stone/20 hover:border-brass/50 transition-all duration-500"
              style={`animation-delay: ${300 + index * 100}ms`}
            >
              <!-- Hover background -->
              <div class="absolute inset-0 bg-gradient-to-r from-brass/5 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>

              <span class="relative text-xl font-display text-cream group-hover:text-brass transition-colors duration-300">
                {cat.name}
              </span>

              <div class="relative flex items-center gap-3">
                <span class="w-0 group-hover:w-8 h-px bg-brass transition-all duration-500"></span>
                <svg
                  class="w-5 h-5 text-stone group-hover:text-brass transition-colors duration-300"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M17 8l4 4m0 0l-4 4m4-4H3" />
                </svg>
              </div>

              <!-- Corner accents -->
              <div class="absolute top-0 left-0 w-3 h-3 border-l border-t border-stone/30 group-hover:border-brass/50 transition-colors duration-300"></div>
              <div class="absolute bottom-0 right-0 w-3 h-3 border-r border-b border-stone/30 group-hover:border-brass/50 transition-colors duration-300"></div>
            </a>
          ))}
        </div>

        <!-- Decorative element -->
        <div class="hidden lg:flex items-center gap-4 mt-8 pl-6 animate-fade-in delay-600">
          <div class="w-12 h-px bg-stone/50"></div>
          <span class="text-ash text-sm tracking-widest uppercase">Est. 2024</span>
        </div>
      </div>
    </div>
  </div>

  <!-- Scroll indicator -->
  <div class="absolute bottom-8 left-1/2 -translate-x-1/2 animate-fade-in delay-500">
    <div class="flex flex-col items-center gap-2 text-ash">
      <span class="text-xs tracking-widest uppercase">Scroll</span>
      <div class="w-px h-8 bg-gradient-to-b from-ash to-transparent"></div>
    </div>
  </div>
</section>
